esphome:
  name: esphome-web-54bafc
  friendly_name: ESPHome BLE Proxy 1

esp32:
  board: esp32dev

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

logger:

api:
  encryption:
    key: !secret api_key

ota:
  - platform: esphome
    password: !secret ota_password

substitutions:
  device_name: "esphome-web-54bafc"

packages:
  ble_proxy: !include ble-proxy-base.yaml
